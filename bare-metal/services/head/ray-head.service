[Unit]
Description=Ray service on head node

[Service]
Type=simple
Restart=always
RestartSec=10
ExecStart=%h/.miniconda/bin/conda run -n ray ray start --head --block --dashboard-host='0.0.0.0'

[Install]
WantedBy=default.target
